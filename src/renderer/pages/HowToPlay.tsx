import { Link } from 'react-router-dom';
import { Button } from '../components';

export const HowToPlay = () => {
  return (
    <>
      <main>
        <Button>
          <Link to="/">Voltar</Link>
        </Button>
      </main>
    </>
  );
};
